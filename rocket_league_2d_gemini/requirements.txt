# For pip: pip install -r requirements.txt
# With uv, `uv sync` installs the same dependencies from pyproject.toml / uv.lock (keep them in step).
gymnasium>=1.2.3
laya==0.3.5
numpy>=2.4.2
pygame>=2.6.1
stable-baselines3[extra]>=2.7.1
torch>=2.10.0
